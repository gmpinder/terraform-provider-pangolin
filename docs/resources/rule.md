---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pangolin_rule Resource - pangolin"
subcategory: ""
description: |-
  Manages a rule for a resource.
---

# pangolin_rule (Resource)

Manages a rule for a resource.

## Example Usage

```terraform
resource "pangolin_rule" "example" {
  resource_id = pangolin_resource.example.id
  action      = "ALLOW"
  match       = "CIDR"
  value       = "100.0.0.0/24"
  priority    = 1
}

resource "pangolin_resource" "example" {
  org_id    = "your-org-id"
  name      = "Example App Resource"
  protocol  = "tcp"
  http      = true
  subdomain = "example-app"
  domain_id = "your-domain-id"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `action` (String) The action to perform for the match.
- `match` (String) What category to match on.
- `priority` (Number) The priority to evaluate the rules. Lower number is evaluated first.
- `resource_id` (Number) The ID of the resource this target belongs to.
- `value` (String) The value to match.

### Optional

- `enabled` (Boolean)

### Read-Only

- `id` (Number) The ID of this resource.
